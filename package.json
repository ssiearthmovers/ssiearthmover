{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "BASE_PATH=/ PORT=3000 NODE_ENV=production npm run build -w @workspace/shiv-shakti && npm run build -w @workspace/api-server && cp -r artifacts/shiv-shakti/dist/public artifacts/api-server/dist/public",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck --workspaces --if-present"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
